[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "db: needs a reachable Postgres (DATABASE_URL) with the Drizzle migration applied",
]
filterwarnings = [
    "ignore::FutureWarning",
    "ignore::DeprecationWarning",
]
